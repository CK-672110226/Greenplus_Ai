# Path to YOLO model weights. Defaults to yolo11n.pt (auto-downloaded).
# Swap to a custom fine-tuned model:  YOLO_MODEL_PATH=./custom_waste.pt
YOLO_MODEL_PATH=yolo11n.pt

# Minimum detection confidence to accept (0.0 – 1.0)
CONF_THRESHOLD=0.30
